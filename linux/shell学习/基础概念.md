- **路径 (Path)：**
    
    - **绝对路径：** 从根目录 `/` 开始的完整路径（例如：`/Users/name/Documents`）。
        
    - **相对路径：** 相对于你当前所在位置的路径（例如：`./Documents` 或 `../Downloads`）。
        
- **特殊目录符号：**
    
    - `.` 代表当前目录。
        
    - `..` 代表上一级目录（父目录）。
        
    - `~` 代表你当前用户的家目录（Home Directory）。