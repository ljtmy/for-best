
- 11:14 
	# range用法
	`range` 的返回值数量取决于你遍历的数据结构。通常它会返回两个值（索引/键 和 对应的值），但你也可以选择只接收一个值或不接收。
	
	### 数组 (Array) 和 切片 (Slice)
	
	- **返回值**：第一个是索引（`index`），第二个是元素值的副本（`value`）
	
	### 字符串 (String)
	
	- **返回值**：第一个是字节索引（`index`），第二个是字符的 `rune` 类型（Unicode 码点）。
	    
	- **特性**：`range` 会自动按 UTF-8 解码字符串。如果包含多字节字符（如中文），**索引可能会跳跃**。
	### ③ 映射 (Map)
	
	- **返回值**：第一个是键（`key`），第二个是值（`value`）。
	    
	- **特性**：Map 的遍历是**无序的**，每次运行结果可能都不同。
	### ④ 通道 (Channel)
	
	- **返回值**：只有一个，即从 Channel 中接收到的数据。
	    
	- **特性**：循环会一直阻塞并等待读取数据，**直到该 Channel 被显式关闭（`close`）**，循环才会自动退出。
	### ⑤ 整数 (Integer) —— Go 1.22+ 新特性
	
	- **用法**：可以直接 `range` 一个整数 $n$，它会从 $0$ 循环到 $n-1$。 