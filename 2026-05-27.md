
- 09:35 
	# map
	## map的创建初始化
	```go
	var m map[string]int   // m 是 nil
	
	m := make(map[string]int)      // 空 map，可以写入
	m["hello"] = 1
	
	m := map[string]int{
	    "apple":  5,
	    "banana": 3,
	}
	
	m := map[string]int{}   // 空的非 nil map，等价于 make(map[string]int)
	
	m := make(map[string]map[string]int)
	// 内层 map 也需要 make
	m["fruit"] = make(map[string]int)
	m["fruit"]["apple"] = 5
	
	```
	## map的常见操作
	```go
	m := make(map[string]int)
	
	// 插入或更新
	m["a"] = 1
	
	// 读取
	v := m["a"]          // 若不存在返回零值
	
	// 判断键是否存在
	v, ok := m["b"]      // ok == false
	
	// 删除
	delete(m, "a")
	
	// 遍历
	for key, value := range m {
	    fmt.Println(key, value)
	}
	
	// 长度
	len(m)
	``` 